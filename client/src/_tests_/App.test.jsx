import { render, screen } from '@testing-library/react'
import '@testing-library/jest-dom'

// Simple test file to get started
describe('EZComm App', () => {
  test('placeholder test', () => {
    expect(true).toBe(true)
  })
})